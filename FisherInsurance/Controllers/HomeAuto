using Microsoft.AspNetCore.Mvc;

public class AutoController : Controller

{
   app.UseMvc(routes =>
        {
            routes.MapRoute(
                            name: "default", 
                            template:
            "{controller=Home}/{action=Index}/{id?}");
   });

}